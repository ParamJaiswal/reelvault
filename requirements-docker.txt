fastapi
uvicorn[standard]
pydantic
pydantic-settings
httpx
yt-dlp
rapidocr-onnxruntime
fastembed
# 12.1.0 introduced get_flattened_data(), used by media.phash(); older
# Pillow raises AttributeError, which sample_frames swallows per-frame and
# frame de-duplication silently stops working.
pillow>=12.1.0
numpy
python-multipart
python-dateutil
cryptography
# v2 multi-source: article body extraction (Apache-2.0) + OA PDF text
# (BSD-3). PyMuPDF deliberately avoided: AGPL blocks APK shipping.
trafilatura==2.2.0
pypdf==6.19.0
